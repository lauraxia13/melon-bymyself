name: Melon Ticket Check (No Slack)

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check Melon Ticket Page
        id: check
        run: |
          curl -s 'https://ticket.melon.com/performance/index.htm?prodId=212678' > page.html
          if grep -q "예매하기" page.html; then
            echo "available=true" >> $GITHUB_OUTPUT
          else
            echo "available=false" >> $GITHUB_OUTPUT
          fi

      - name: Create GitHub Issue if available
        if: steps.check.outputs.available == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: "🎫 Melon 有票了！",
              body: "检测到 Melon 页面出现【예매하기】，请立刻前往抢票！"
            })
              repo: context.repo.repo,
              title: "🎫 Melon 有票提醒",
              body: "检测到 Melon 有票了，快去官网抢！"
            });
